#include "Lights.h"
#include "sampling.h"
#include <complex>

namespace pathtracer
{

    Spectrum L(vec3 &coordinate, vec3 &wo, const vec3 &w){ 
        //return Dot(intr.n, w) > 0.f ? Lemit : Spectrum(0.f);
    } 
}
